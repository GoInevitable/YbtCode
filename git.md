Git 快速参考手册
==========

核心概念
----

* **工作区**：你直接编辑文件的地方。

* **暂存区**：临时存放你的改动 (`git add`)。

* **本地仓库**：存放提交记录的地方 (`git commit`)。

* **远程仓库**：服务器上的仓库，用于团队共享 (`git push`)。

基础工作流
-----------

### 1. 初始化与克隆

```bash
# 在当前目录创建新仓库
git init

# 下载远程仓库到本地
git clone <仓库URL>
```

### 2. 日常修改与提交

```bash
# 检查哪些文件被修改了
git status

# 将文件的修改添加到暂存区
git add <文件名>
git add .                # 添加所有修改

# 将暂存区的改动正式提交到本地仓库
git commit -m "清晰的提交说明"
```

团队协作流程
------

### 1. 分支管理（功能开发标准流程）

```bash
# 查看所有分支（*号标注当前分支）
git branch

# 创建并切换到一个新分支（用于新功能或修复）
git checkout -b <新分支名>

# ... 在此分支上完成你的工作 (add & commit) ...

# 切换回主分支（如 main 或 master）
git checkout main

# 将开发好的分支合并到主分支
git merge <分支名>

# 删除已合并的分支
git branch -d <分支名>
```

### 2. 与远程仓库同步

```bash
# 首次推送本地分支到远程，并建立关联
git push -u origin <分支名>

# 后续推送（在对应分支上执行）
git push

# 从远程仓库获取最新代码并合并到当前分支
git pull

# 仅获取远程更新，不自动合并（更安全）
git fetch
git merge origin/main    # 手动合并
```

常用状态查询
------

```bash
# 查看提交历史（简洁版）
git log --oneline

# 查看工作区和暂存区的文件差异
git diff

# 查看远程仓库地址
git remote -v
```

⚡️ 黄金法则
-------

1. **提交前先拉取**：在 `push`之前，先执行 `git pull`获取队友的最新代码，避免冲突。

2. **勤提交**：完成一个小功能或修复就提交一次，提交信息要清晰。

3. **开分支**：任何新功能或修复都应创建新分支，不要直接在 `main`分支上工作。

4. **忽略文件**：将编译产物、配置文件等写入 `.gitignore`文件，避免误提交。

* * *

> **遇到问题？**
> 
> * 查看详细帮助：`git help <命令>`
> 
> * 撤销工作区的修改：`git checkout -- <文件名>`(谨慎使用！)
> 
> * 查看所有操作记录（用于找回误删的提交）：`git reflog`


USING AI

